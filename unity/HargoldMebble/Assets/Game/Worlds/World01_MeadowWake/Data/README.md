# Data

The authoritative opening-slice layout is repository-level data at
`data/level-art/world-1/meadow-wake-opening-layout.json`. The Unity editor import
tool reads that file directly so no second editable copy can drift.
